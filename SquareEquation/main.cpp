#include <iostream>

int main()
{
	float a{ 3 }, b{ 5 }, c{ 2 };

//	std::cin >> a, b, c;
	std::cout << "ax2 + bx + c = 0" << std::endl << "a, b, c" << std::endl;
	cin >> b >> c;
	// d- дискриминант епта
	D = b * b - 4 * a * c;
	std::cout << "ну тут чему равен дискриминант получается" << D << std::endl;
	std::cout(D<0)


	 

	return 0;
}

